# A
```c
#include <iostream>
#include<algorithm>
#include <cstring>
using namespace std;
const int N = 1010;
#define int long long
void solve(){
    int n,a[N];
    cin >> n;for(int i = 1;i <= n;i++)cin >> a[i];
}
signed main(){int _;cin >> _;while(_--)solve();return 0;}
```
